import React from "react";
import { StubScreen } from "../_StubScreen";
export function ZeniCardsScreen() {
  return <StubScreen title="ZeniCards" subtitle="Virtual cards for agent spend. Issue + reveal PAN behind Face ID — mobile UI ships next." />;
}
