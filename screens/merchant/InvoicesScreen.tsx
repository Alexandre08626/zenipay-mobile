import React from "react";
import { StubScreen } from "../_StubScreen";
export function InvoicesScreen() {
  return <StubScreen title="Invoices" subtitle="Create, track, and collect invoices. Use the web dashboard for now." />;
}
