import React from "react";
import { StubScreen } from "../_StubScreen";
export function PaymentLinksScreen() {
  return <StubScreen title="Payment links" subtitle="Create shareable checkout URLs. Full mobile UI ships next." />;
}
